/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        domains: ['images-na.ssl-images-amazon.com', 'i.pinimg.com'], // Add the domain here
    },
};

export default nextConfig;
