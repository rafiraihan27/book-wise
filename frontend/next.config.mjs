/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        domains: ['images-na.ssl-images-amazon.com', 'nuriakenya.com', 'cdn2.penguin.com.au', 'i.pinimg.com', 'covers.shakespeareandcompany.com', 'img.freepik.com'], // Add the domain here
    },
};

export default nextConfig;
